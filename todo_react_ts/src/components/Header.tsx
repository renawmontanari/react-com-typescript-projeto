import React from "react";

import styles from "./Header.module.css";

const Header = () => {
  return (
    <header clasName={styles.header}>
      <h1>Todo React com Typescript</h1>
    </header>
  );
};

export default Header;
